import {sign} from "../components"
import {back} from "../components"
export const SignUp = () => {
    return(
        <>
        <back.Back/>
        <h1 className=" title ">Sign-up</h1>
        <sign.SSigning />
        </>
    );
}
